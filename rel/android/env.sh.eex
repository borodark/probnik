#!/bin/sh
# Android release environment

export RELEASE_DISTRIBUTION=name
export RELEASE_NODE=probnik@localhost

# These will be set by the Android native code
# export RELEASE_ROOT=/data/data/com.probnik.app/files/release
# export RELEASE_TMP=/data/data/com.probnik.app/cache
